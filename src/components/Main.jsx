import Card from "./Card"

const Main = () => {
    return (
        <main>
            <button className="addBtn">Add</button>
            <div className="cards__container">
                < Card />
            </div>
        </main>
    )
}

export default Main